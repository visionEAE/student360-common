<?xml version="1.0" encoding="UTF-8"?>
<!-- Operational log for every Student 360 service: one JSON object per line on stdout, which is
     exactly what Cloud Logging ingests in stage 2 without a code change. Picked up automatically
     by any service that has this library on its classpath and ships no logback config of its own. -->
<configuration>
  <springProperty scope="context" name="serviceName" source="student360.service-name" defaultValue="unknown"/>

  <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
      <fieldNames>
        <timestamp>timestamp</timestamp>
        <version>[ignore]</version>
        <levelValue>[ignore]</levelValue>
        <logger>logger</logger>
        <thread>thread</thread>
      </fieldNames>
      <customFields>{"service":"${serviceName}"}</customFields>
      <!-- Only the correlation keys; nothing else that lands in the MDC leaks into the log. -->
      <includeMdcKeyName>requestId</includeMdcKeyName>
      <includeMdcKeyName>traceId</includeMdcKeyName>
      <includeMdcKeyName>spanId</includeMdcKeyName>
      <includeMdcKeyName>userId</includeMdcKeyName>
      <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
        <maxDepthPerThrowable>20</maxDepthPerThrowable>
        <rootCauseFirst>true</rootCauseFirst>
      </throwableConverter>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="JSON"/>
  </root>
</configuration>
