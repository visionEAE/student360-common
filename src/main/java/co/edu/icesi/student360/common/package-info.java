/**
 * Shared foundations for every Student 360 service: structured logging and correlation, identity
 * context, audit trail, outbox events and service-to-service tokens. Everything that becomes a
 * managed cloud service in stage 2 is exposed here as a port with a local adapter.
 */
package co.edu.icesi.student360.common;
